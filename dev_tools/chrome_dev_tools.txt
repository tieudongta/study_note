MailTag
Use F12 to open DevTool on Chrome
-way to dock the devtool panel
-Reponsive view: use toggle device tool bar or Ctrl+Shift+M
-Elements (inspector)
  + click checkbox to toggle the element
  + Edit text in elements
  + Remove element from DOM
  + Change status of element
  + Use color picker
  + Add new style
-Console
	+ able to run javascript
	+ clear the console use button
	+ $_ to get the last value
	+ $0 to get the last element selected $1 $2 and so on
	+ use JQuery syntax to manipulate the DOM
	+ use console alongside with vs code
	+ function javascript: console.log(); console.error(); console.warn();console.dir(); console.table(name:;John', email:'test@test.com')//create a table on console; console.clear()//clear the content in console; console.group() and console.groupEnd() //grouping a list of command
	+ js functions 2: console.time() and console.timeEnd()//tell time executing a certain function
	+ js functions 3: console.
	+ able to test a js function
	+ create snippet
-Sources
	+create snippet
	+ work with codespace
-Network
	+ show all files using in the URL
	+ WS: web Socket
	+ Headers, Preview, Respone, Cookies, Timing
	+ XHR: infor from Ajax call,
- Application
	+ Storage, Cache, Frames
	+ Local Storage vs Session Storage
		localeStorage.setItem(), localStorage.clear() //store in local
		sessionStorage.setItem(); //go away when browser closed
- Audit:
	+ see the performance and suggestion for improvement
	+ run audit to get results
- Security:
